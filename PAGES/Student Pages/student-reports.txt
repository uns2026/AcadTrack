<!-- ===== EXPLICIT LAYOUT OVERRIDE ===== -->
<style>
  .article__header {
    margin-bottom: var(--space-xl);
    padding-bottom: var(--space-lg);
    border-bottom: 1px solid var(--border-light);
    display: none;
  }
  .layout {
    display: contents;
    gap: var(--space-xl);
  }
  .article {
    max-width: -webkit-fill-available;
    margin: -27px auto;
  }

  /* Hide Blogger Default wrappers */
  .sidebar, .page-title, .sidebar-widget { display: none !important; }
  .layout { grid-template-columns: 1fr !important; }
  .site-main { display: block !important; padding: 0 !important; }
</style>

<!-- ===== GLOBAL STYLES ===== -->
<style>
  :root {
    --primary: #2563eb;
    --primary-hover: #1d4ed8;
    --text: #0f172a;
    --text-secondary: #475569;
    --text-light: #94a3b8;
    --bg: #ffffff;
    --bg-cool: #f8fafc;
    --bg-warm: #f1f5f9;
    --border: #cbd5e1;
    --border-light: #e2e8f0;
    --card: #ffffff;
    --radius: 12px;
    --radius-sm: 8px;
    --radius-lg: 16px;
    --shadow-sm: 0 1px 3px rgba(0,0,0,0.05);
    --shadow: 0 4px 6px -1px rgba(0,0,0,0.05), 0 2px 4px -1px rgba(0,0,0,0.03);
    --shadow-lg: 0 10px 15px -3px rgba(0,0,0,0.05), 0 4px 6px -2px rgba(0,0,0,0.05);
    --space-xs: 0.5rem;
    --space-sm: 1rem;
    --space-md: 1.5rem;
    --space-lg: 2rem;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    font-family: 'Cairo', -apple-system, BlinkMacSystemFont, sans-serif;
    font-size: 14px;
    line-height: 1.6;
    color: var(--text);
    background-color: var(--bg-cool);
    -webkit-font-smoothing: antialiased;
  }
  input, button, select { font: inherit; color: inherit; }
  a { color: inherit; text-decoration: none; }

  /* Stretch main container to fill full viewport width */
  .saas-container {
    width: 100% !important;
    max-width: 100% !important;
    margin: 0 !important;
    padding: 32px 28px !important;
  }
  @media (max-width: 1023px) {
    .saas-container { padding: 20px 16px !important; }
  }

  /* Page Header */
  .saas-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 16px;
    margin-bottom: var(--space-lg);
    padding-bottom: var(--space-sm);
    border-bottom: 1.5px solid var(--border-light);
  }
  .saas-header h1 {
    font-size: 26px;
    font-weight: 800;
    color: var(--text);
    letter-spacing: -0.03em;
  }

  /* ===== REPORT TEMPLATE CARDS ===== */
  .rep-template-grid { display: grid; gap: 16px; grid-template-columns: repeat(1, 1fr); margin-bottom: var(--space-md); }
  @media (min-width: 640px) { .rep-template-grid { grid-template-columns: repeat(3, 1fr); } }
  .rep-template-card { background: #ffffff; border: 1.5px solid #e2e8f0; border-radius: var(--radius); padding: 18px; cursor: pointer; transition: all 0.2s ease; display: flex; align-items: flex-start; gap: 12px; position: relative; overflow: hidden; text-align: start; }
  .rep-template-card:hover { transform: translateY(-2px); border-color: var(--text-light); box-shadow: var(--shadow); }
  .rep-template-card.selected { border-color: #2563eb; background-color: #eff6ff; box-shadow: 0 4px 12px rgba(37,99,235,0.08); }
  .rep-template-card::before { content: ''; position: absolute; left: 0; top: 0; bottom: 0; width: 4px; background: transparent; transition: background 0.2s; }
  .rep-template-card.selected::before { background: #2563eb; }
  
  .rep-card-icon { width: 40px; height: 40px; border-radius: 10px; background-color: #f1f5f9; color: #64748b; display: flex; align-items: center; justify-content: center; font-size: 16px; flex-shrink: 0; transition: all 0.2s; }
  .rep-template-card.selected .rep-card-icon { background-color: #2563eb; color: #ffffff; }
  .rep-card-details { flex: 1; min-width: 0; }
  .rep-card-title { font-size: 13.5px; font-weight: 800; color: #0f172a; margin-bottom: 2px; }
  .rep-card-desc { font-size: 11px; color: #64748b; line-height: 1.4; }

  /* ===== FORMAT PILL SWITCHERS ===== */
  .format-pills { display: flex; gap: 8px; margin-top: 4px; }
  .format-pill { padding: 8px 16px; border: 1.5px solid #cbd5e1; background: #ffffff; color: #475569; font-size: 12px; font-weight: 700; cursor: pointer; border-radius: 8px; transition: all 0.2s ease; display: inline-flex; align-items: center; gap: 6px; }
  .format-pill:hover { background-color: #f1f5f9; }
  .format-pill.active { background-color: #2563eb; color: #ffffff; border-color: #2563eb; }

  /* ===== FILE FORMAT BADGES ===== */
  .format-badge { font-size: 10px; font-weight: 800; padding: 3px 8px; border-radius: 4px; text-transform: uppercase; letter-spacing: 0.05em; display: inline-block; }
  .format-badge.csv { background-color: #ecfdf5; color: #047857; border: 1px solid #a7f3d0; }
  .format-badge.pdf { background-color: #fef2f2; color: #b91c1c; border: 1px solid #fecaca; }
  .format-badge.excel { background-color: #fffbeb; color: #b45309; border: 1px solid #fde68a; }

  /* ===== TABLES ===== */
  .table-card { background: var(--card); border: 1.5px solid var(--border-light); border-radius: var(--radius); box-shadow: var(--shadow-sm); overflow: hidden; margin-bottom: var(--space-md); max-width: 100%; width: 100%; }
  .table-responsive { width: 100%; overflow-x: auto; scrollbar-width: thin; -webkit-overflow-scrolling: touch; display: block; }
  .saas-table { width: 100%; border-collapse: collapse; text-align: left; font-size: 13.5px; min-width: 650px; }
  .saas-table th { background-color: var(--bg-cool); padding: 12px 18px; font-weight: 700; color: var(--text-secondary); border-bottom: 1.5px solid var(--border-light); }
  .saas-table td { padding: 14px 18px; border-bottom: 1px solid var(--border-light); color: var(--text); vertical-align: middle; }
  .saas-table tr:last-child td { border-bottom: none; }
  .saas-table tr:hover td { background-color: var(--bg-cool); }

  /* ===== COMPILER LOADING SPINNER MODAL ===== */
  .std-modal-overlay { position: fixed; inset: 0; background: rgba(15,23,42,0.5); backdrop-filter: blur(4px); z-index: 99999; display: none; align-items: center; justify-content: center; opacity: 0; transition: all 0.25s ease; }
  .std-modal-overlay.active { display: flex; opacity: 1; }
  .std-modal-box { background: #ffffff; border-radius: 16px; width: 90%; max-width: 340px; box-shadow: 0 25px 50px rgba(0,0,0,0.25); padding: 24px; text-align: center; }
  .spinner { width: 44px; height: 44px; border: 4px solid #e2e8f0; border-top-color: #2563eb; border-radius: 50%; animation: spin 1s linear infinite; margin: 0 auto 16px; }
  @keyframes spin { to { transform: rotate(360deg); } }

  /* ===== PORTABLE FLOATING TOAST NOTIFIER (TOP-LEFT) ===== */
  .profile-toast-container { position: fixed; top: 80px; left: 20px; display: flex; flex-direction: column; gap: 8px; z-index: 1000000; max-width: 340px; width: calc(100% - 40px); pointer-events: none; }
  .saas-toast-alert { background: #ffffff; border: 1.5px solid var(--border-light); border-left: 4px solid #10b981; border-radius: 10px; padding: 12px 16px; box-shadow: 0 10px 15px -3px rgba(15, 23, 42, 0.15); display: flex; align-items: center; gap: 12px; transform: translateX(-120%); transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.3s ease; opacity: 0; pointer-events: auto; }
  .saas-toast-alert.active { transform: translateX(0); opacity: 1; }
  .saas-toast-icon { width: 32px; height: 32px; border-radius: 50%; background: #ecfdf5; color: #10b981; display: flex; align-items: center; justify-content: center; font-size: 14px; flex-shrink: 0; }
  .saas-toast-body { display: flex; flex-direction: column; gap: 2px; text-align: left; }
  .saas-toast-title { font-size: 13px; font-weight: 800; color: var(--text); }
  .saas-toast-desc { font-size: 11px; color: var(--text-secondary); font-weight: 500; }

  /* ===== BUTTONS ===== */
  .btn-saas { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 10px 18px; border-radius: var(--radius-sm); font-size: 13px; font-weight: 600; cursor: pointer; border: 1px solid transparent; transition: all 0.2s ease; font-family: inherit; }
  .btn-saas-primary { background-color: #2563eb; color: white !important; }
  .btn-saas-primary:hover { background-color: #1d4ed8; }
  .btn-saas-secondary { background-color: var(--bg-cool); color: var(--text-secondary); border-color: var(--border); }
  .btn-saas-secondary:hover { background-color: var(--bg-warm); color: var(--text); }
  .btn-icon-only { width: 34px; height: 34px; padding: 0; border-radius: 8px; display: inline-flex; align-items: center; justify-content: center; font-size: 13px; border: none; cursor: pointer; background: #2563eb; color: white; }

  /* ===== SECTION DIVIDER ===== */
  .section-divider { display: flex; align-items: center; gap: 10px; margin: var(--space-md) 0 var(--space-sm); }
  .section-divider-label { font-size: clamp(10px, 1vw, 11px); font-weight: 700; color: #475569; text-transform: uppercase; letter-spacing: 0.08em; white-space: nowrap; }
  .section-divider-line { flex: 1; height: 1px; background: #e2e8f0; }

  /* ===== RTL ALIGNMENTS ===== */
  .lang-ar .saas-header, .lang-ar .form-card, .lang-ar .saas-table { direction: rtl; text-align: right; }
  .lang-ar .saas-table th, .lang-ar .saas-table td { text-align: right; }
  .lang-ar .rep-template-card { text-align: right !important; direction: rtl !important; display: flex !important; justify-content: flex-start !important; gap: 12px !important; }
  .lang-ar .rep-template-card::before { left: auto; right: 0; }
  .lang-ar .rep-template-card.selected::before { background: #2563eb; }
  .lang-ar .format-pills { flex-direction: row-reverse !important; }
  .lang-ar .format-pill { flex-direction: row-reverse !important; }
  .lang-ar .btn-saas-primary i { transform: scaleX(-1); }
  .lang-ar .section-divider { flex-direction: row-reverse; }
  .lang-ar .profile-toast-container { left: auto; right: 20px; }
  .lang-ar .saas-toast-alert { border-left: none; border-right: 4px solid #10b981; transform: translateX(120%); flex-direction: row-reverse; }
  .lang-ar .saas-toast-alert.active { transform: translateX(0); }
  .lang-ar .saas-toast-body { text-align: right; }
</style>

<div class="saas-container">
  
  <!-- Floating Toast Notifier (Top-Left on LTR) [1] -->
  <div class="profile-toast-container" id="profileToastContainer"></div>

  <header class="saas-header">
    <h2>
      <span data-lang="en">My Academic Documents Center</span>
      <span data-lang="fr">Mes Documents Académiques</span>
      <span data-lang="ar">مركز استخراج وثائق الطالب والمحاضر</span>
    </h2>
    <div style="display:flex; gap:8px;">
      <button class="btn-saas btn-saas-secondary" onclick="window.location.href='/p/student-dashboard.html'"><i class="fa-solid fa-arrow-left"></i> <span data-lang="en">Back to Dashboard</span><span data-lang="fr">Retour</span><span data-lang="ar">العودة للوحة التحكم</span></button>
    </div>
  </header>

  <!-- Step 1: Select Report Template (Cards) -->
  <div class="section-divider">
    <span class="section-divider-label"><span data-lang="en">1. Select Document Template</span><span data-lang="fr">1. Choisir le Document</span><span data-lang="ar">1. اختر نموذج الوثيقة المطلوبة</span></span>
    <div class="section-divider-line"></div>
  </div>

  <div class="rep-template-grid">
    <!-- Template 1: S2 Academic Transcript -->
    <div class="rep-template-card selected" id="cardRepGrades" onclick="selectReportTemplate('Evaluations Summary', 'cardRepGrades')">
      <div class="rep-card-icon"><i class="fa-solid fa-square-poll-vertical"></i></div>
      <div class="rep-card-details">
        <div class="rep-card-title">
          <span data-lang="en">S2 Academic Transcript</span><span data-lang="fr">Relevé de Notes S2</span><span data-lang="ar">كشف نقاط السداسي الثاني</span>
        </div>
        <div class="rep-card-desc">
          <span data-lang="en">Official grade transcript showing S2 exam, TD, and TP evaluation averages.</span>
          <span data-lang="fr">Relevé officiel présentant les notes d'examens, TD et TP de vos modules.</span>
          <span data-lang="ar">كشف النقاط الرسمي للفصل الثاني لرصد درجات الامتحانات والأعمال الموجهة ومعدلات المقاييس.</span>
        </div>
      </div>
    </div>

    <!-- Template 2: Certificate of Enrollment -->
    <div class="rep-template-card" id="cardRepEnrollment" onclick="selectReportTemplate('Certificate of Enrollment', 'cardRepEnrollment')">
      <div class="rep-card-icon"><i class="fa-solid fa-graduation-cap"></i></div>
      <div class="rep-card-details">
        <div class="rep-card-title">
          <span data-lang="en">Certificate of Enrollment</span><span data-lang="fr">Certificat de Scolarité</span><span data-lang="ar">شهادة مدرسية لإثبات التسجيل</span>
        </div>
        <div class="rep-card-desc">
          <span data-lang="en">Official enrollment certificate verifying active third-year status (L3) in psychology.</span>
          <span data-lang="fr">Certificat officiel validant votre inscription active en 3ème année (L3).</span>
          <span data-lang="ar">الشهادة المدرسية الرسمية التي تثبت تسجيلك كطالب نظامي بالسنة الثالثة (L3) بقسم علم النفس.</span>
        </div>
      </div>
    </div>

    <!-- Template 3: Attendance Certificate -->
    <div class="rep-template-card" id="cardRepAttendance" onclick="selectReportTemplate('Attendance Registry', 'cardRepAttendance')">
      <div class="rep-card-icon"><i class="fa-solid fa-calendar-check"></i></div>
      <div class="rep-card-details">
        <div class="rep-card-title">
          <span data-lang="en">Diligence &amp; Attendance Certificate</span><span data-lang="fr">Certificat d'Assiduité</span><span data-lang="ar">شهادة المواظبة وحضور الحصص</span>
        </div>
        <div class="rep-card-desc">
          <span data-lang="en">Official certificate declaring your current calculated attendance rate of 87.5% in S2.</span>
          <span data-lang="fr">Attestation officielle validant votre taux de présence semestriel de 87.5%.</span>
          <span data-lang="ar">شهادة رسمية تصدر من القسم لتأكيد نسبة حضور ومواظبة الطالب للفصل الحالي البالغة 87.5%.</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Step 2: Configure Parameters & Format -->
  <div class="section-divider">
    <span class="section-divider-label"><span data-lang="en">2. Configure Parameters &amp; Format</span><span data-lang="fr">2. Configurer les Paramètres</span><span data-lang="ar">2. حدد معلمات التصدير والملف</span></span>
    <div class="section-divider-line"></div>
  </div>

  <div class="form-card">
    <form class="form-grid" id="reports-generator-fields" onsubmit="event.preventDefault(); compileSaaSReport();">
      <div class="form-group">
        <label class="form-label"><span data-lang="en">Export Format</span><span data-lang="fr">Format d'exportation</span><span data-lang="ar">صيغة الملف المصدر</span></label>
        <div class="format-pills">
          <button type="button" class="format-pill active" id="pill-csv" onclick="setExportFormat('csv')"><i class="fa-solid fa-file-csv"></i> CSV (Spreadsheet)</button>
          <button type="button" class="format-pill" id="pill-pdf" onclick="setExportFormat('pdf')"><i class="fa-solid fa-file-pdf"></i> PDF (Print Layout)</button>
          <button type="button" class="format-pill" id="pill-excel" onclick="setExportFormat('excel')"><i class="fa-solid fa-file-excel"></i> Excel (XLSX Format)</button>
        </div>
      </div>
      <div style="text-align: right; margin-top: 10px;">
        <button class="btn-saas btn-saas-primary" type="submit">
          <i class="fa-solid fa-gears"></i>
          <span data-lang="en">Compile Document</span><span data-lang="fr">Compiler</span><span data-lang="ar">استخراج وتوليد الوثيقة</span>
        </button>
      </div>
    </form>
  </div>

  <!-- Step 3: Compilation Log History (100% Hardcoded Demo Logs) [1] -->
  <div class="section-divider">
    <span class="section-divider-label"><span data-lang="en">Compiled Documents (statically pre-filled)</span><span data-lang="fr">Historique</span><span data-lang="ar">سجلات الوثائق المستخرجة مسبقاً (مملوءة كلياً)</span></span>
    <div class="section-divider-line"></div>
  </div>

  <div class="table-card">
    <div class="table-responsive">
      <table class="saas-table" id="saas-reports-log-table">
        <thead>
          <tr>
            <th style="width: 110px;">Document Ref</th>
            <th>Scope / Template</th>
            <th>Format</th>
            <th>Timestamp</th>
            <th style="text-align: center; width: 64px;">Download</th>
          </tr>
        </thead>
        <tbody>
          <!-- Log 1 -->
          <tr>
            <td style="font-family:var(--font-mono); font-weight:700; color:var(--text);">DOC_48210</td>
            <td style="font-weight:700;">Certificate of Enrollment</td>
            <td><span class="format-badge pdf">pdf</span></td>
            <td style="font-family:var(--font-mono);">2026-05-25 10:30:15</td>
            <td style="text-align:center;"><button class="btn-saas btn-saas-secondary btn-icon-only" onclick="downloadSaaSReportArchive('DOC_48210')"><i class="fa-solid fa-cloud-arrow-down"></i></button></td>
          </tr>
          <!-- Log 2 -->
          <tr>
            <td style="font-family:var(--font-mono); font-weight:700; color:var(--text);">DOC_10925</td>
            <td style="font-weight:700;">Attendance Registry</td>
            <td><span class="format-badge csv">csv</span></td>
            <td style="font-family:var(--font-mono);">2026-05-15 15:30:00</td>
            <td style="text-align:center;"><button class="btn-saas btn-saas-secondary btn-icon-only" onclick="downloadSaaSReportArchive('DOC_10925')"><i class="fa-solid fa-cloud-arrow-down"></i></button></td>
          </tr>
          <!-- Log 3 -->
          <tr>
            <td style="font-family:var(--font-mono); font-weight:700; color:var(--text);">DOC_09184</td>
            <td style="font-weight:700;">Evaluations Summary</td>
            <td><span class="format-badge pdf">pdf</span></td>
            <td style="font-family:var(--font-mono);">2026-05-12 14:15:02</td>
            <td style="text-align:center;"><button class="btn-saas btn-saas-secondary btn-icon-only" onclick="downloadSaaSReportArchive('DOC_09184')"><i class="fa-solid fa-cloud-arrow-down"></i></button></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Simulated compiling spinner modal overlay -->
<div id="compiling-modal" class="std-modal-overlay">
  <div class="std-modal-box">
    <div class="spinner"></div>
    <div style="font-weight:700; font-size:13.5px; color:var(--text);"><span data-lang="en">Compiling academic records, please wait...</span><span data-lang="fr">Compilation en cours, veuillez patienter...</span><span data-lang="ar">جاري تجميع ومعالجة السجلات الأكاديمية...</span></div>
  </div>
</div>

<script>
//<![CDATA[
  let selectedTemplate = 'Evaluations Summary'; // S2 Transcript active by default
  let selectedFormat = 'csv';

  // 100% Static High-Fidelity Mock Student Database (Imad AZIZI - L3 Clinique A) [1]
  const mockStudentObj = {
    id: "202312001",
    firstName: "Imad",
    lastName: "AZIZI",
    group: "L3 Psychologie Clinique",
    email: "imad.azizi@univ-batna.dz",
    phone: "0659144039",
    att: "87.5%",
    gpa: "14.50"
  };

  const mockS2Grades = [
    { code: "PSY-311", name: "منهجية وتقنيات البحث", exam: "14.50", td: "15.00", tp: "13.50", assignment: "14.00", participation: "15.00", avg: 14.50 },
    { code: "PSY-312", name: "علم النفس المرضي", exam: "13.00", td: "14.00", tp: "14.50", assignment: "14.00", participation: "15.00", avg: 13.50 },
    { code: "PSY-313", name: "نظريات التعلم", exam: "10.00", td: "11.50", tp: "12.00", assignment: "14.00", participation: "15.00", avg: 11.20 },
    { code: "PSY-314", name: "مشكلات نفسية اجتماعية", exam: "15.50", td: "16.00", tp: "13.80", assignment: "14.00", participation: "15.00", avg: 15.10 },
    { id: "PSY-315", name: "علم النفس التنظيم والعمل", exam: "11.50", td: "12.50", tp: "13.50", assignment: "14.00", participation: "15.00", avg: 12.50 }
  ];

  document.addEventListener('DOMContentLoaded', function() {
    const initLang = localStorage.getItem('acadtrack-lang') || 'en';
    setLanguage(initLang);
  });

  function setLanguage(lang) {
    localStorage.setItem('acadtrack-lang', lang);
    document.documentElement.lang = lang;
    const isRTL = lang === 'ar';
    const card = document.querySelector('.saas-container');
    if (card) card.dir = isRTL ? 'rtl' : 'ltr';

    document.querySelectorAll('[data-lang]').forEach(el => {
      el.style.display = el.dataset.lang === lang ? '' : 'none';
    });
  }

  window.selectReportTemplate = function(templateName, cardId) {
    selectedTemplate = templateName;
    document.querySelectorAll('.rep-template-card').forEach(c => c.classList.remove('selected'));
    const activeCard = document.getElementById(cardId);
    if (activeCard) activeCard.classList.add('selected');
  };

  window.setExportFormat = function(format) {
    selectedFormat = format;
    document.querySelectorAll('.format-pill').forEach(p => p.classList.remove('active'));
    const activePill = document.getElementById(`pill-${format}`);
    if (activePill) activePill.classList.add('active');
  };

  window.compileSaaSReport = function() {
    const modal = document.getElementById('compiling-modal');
    modal.classList.add('active');
    setLanguage(localStorage.getItem('acadtrack-lang') || 'en');

    // Simulate 1.2-second compilation process [2]
    setTimeout(() => {
      modal.classList.remove('active');

      if (selectedFormat === 'csv' || selectedFormat === 'excel') {
        compileCSVOrExcelFile(selectedFormat);
      } else if (selectedFormat === 'pdf') {
        compilePDFPrintLayout();
      }

      // Spawn non-blocking top-left green checkmark toast [2]
      triggerSuccessToast("Document Compiled", `Successfully compiled ${selectedTemplate} transcript.`);
    }, 1200);
  };

  function compileCSVOrExcelFile(format) {
    let fileContent = '';
    
    if (selectedTemplate === 'Attendance Registry') {
      fileContent = "Module,Total Classes,Present,Absent,Attendance Rate\n";
      fileContent += "منهجية وتقنيات البحث,3,3,0,100%\n";
      fileContent += "علم النفس المرضي,3,2,1,66.7%\n";
      fileContent += "نظريات التعلم,3,3,0,100%\n";
    } else if (selectedTemplate === 'Evaluations Summary') {
      fileContent = "Module,Exam Grade,TD Grade,TP Grade,Assignment,Participation,Average GPA,Result\n";
      mockS2Grades.forEach(s => {
        fileContent += `"${s.name}",${s.exam},${s.td},${s.tp},${s.assignment},${s.participation},${s.avg.toFixed(2)},Pass\n`;
      });
    } else {
      fileContent = "Document Type,Value\n";
      fileContent += "Enrollment Verification Certificate\n";
      fileContent += "Student Name,Imad AZIZI\n";
      fileContent += "Specialty,Psychologie Clinique (L3)\n";
      fileContent += "Institution,Université Batna 1\n";
    }

    const extension = format === 'csv' ? 'csv' : 'xls';
    const fileBlob = new Blob([fileContent], { type: format === 'csv' ? 'text/csv' : 'application/vnd.ms-excel' });
    
    const link = document.createElement('a');
    link.href = URL.createObjectURL(fileBlob);
    link.download = `my_${selectedTemplate.toLowerCase().replace(/\s+/g,'_')}.${extension}`;
    link.click();
  }

  // Official Algerian administrative printable HTML sheet (Arabic layout, Algerian headers & official Batna 1 University logo) [2]
  function compilePDFPrintLayout() {
    const printWindow = window.open("", "_blank");
    if (!printWindow) {
      alert("Pop-up blocker active. Please allow popups to compile the printable PDF sheet.");
      return;
    }

    let documentContentsHTML = '';
    
    if (selectedTemplate === 'Attendance Registry') {
      documentContentsHTML = `
        <div style="font-size:15px; font-weight:700; color:#334155; margin-bottom:15px; text-align:center;">شهادة حضور ومواظبة الطالب</div>
        <p style="font-size:14px; line-height:1.8; text-align:right;">
          تشهد إدارة كلية العلوم الإنسانية والاجتماعية بجامعة باتنة 1، بناءً على السجلات الرقمية المسجلة في البوابة، أن الطالب <strong>${mockStudentObj.lastName} ${mockStudentObj.firstName}</strong>، المولود بتاريخ <strong>2002/04/12</strong>، والحامل لبطاقة تسجيل رقم <strong>${mockStudentObj.id}</strong>، يزاول دراسته بانتظام في السنة الثالثة (L3) تخصص <strong>علم النفس العيادي</strong>.
        </p>
        <p style="font-size:14px; line-height:1.8; text-align:right; margin-top:10px;">
          وقد بلغت نسبة حضوره ومواظبته الإجمالية على الدروس والأعمال التطبيقية والموجهة للفصل الحالي: <strong style="color:#047857; font-size:16px;">${mockStudentObj.att}</strong>.
        </p>
      `;
    } else if (selectedTemplate === 'Certificate of Enrollment') {
      documentContentsHTML = `
        <div style="font-size:16px; font-weight:800; color:#1e3a8a; margin-bottom:20px; text-align:center;">شهادة تسجيل مدرسية</div>
        <p style="font-size:14px; line-height:1.9; text-align:right;">
          يشهد عميد كلية العلوم الإنسانية والاجتماعية بجامعة باتنة 1، أن الطالب(ة): <strong>${mockStudentObj.lastName} ${mockStudentObj.firstName}</strong><br>
          المولود(ة) في: <strong>12 أفريل 2002</strong> بـ <strong>باتنة</strong><br>
          مسجل(ة) بانتظام بالجامعة برسم السنة الجامعية: <strong>2025/2026</strong><br>
          الطور: <strong>ليسانس (LMD)</strong> — السنة: <strong>السنة الثالثة (L3)</strong> — شعبة: <strong>علم النفس</strong> — تخصص: <strong>علم النفس العيادي</strong>.
        </p>
        <p style="font-size:13px; color:#64748b; text-align:right; margin-top:24px;">
          سلمت هذه الشهادة لتقديمها في حدود ما يسمح به القانون الإداري.
        </p>
      `;
    } else {
      // Evaluations Summary (S2 Transcript)
      const rows = mockS2Grades.map(s => `
        <tr>
          <td style="font-family:monospace; border: 1px solid #cbd5e1; padding:8px; text-align:center;">${s.id || 'PSY-311'}</td>
          <td style="border: 1px solid #cbd5e1; padding:8px; font-weight:bold;">${s.name}</td>
          <td style="border: 1px solid #cbd5e1; padding:8px; text-align:center; font-family:monospace;">${s.exam}</td>
          <td style="border: 1px solid #cbd5e1; padding:8px; text-align:center; font-family:monospace;">${s.td}</td>
          <td style="border: 1px solid #cbd5e1; padding:8px; text-align:center; font-family:monospace;">${s.tp}</td>
          <td style="border: 1px solid #cbd5e1; padding:8px; text-align:center; font-family:monospace;">${s.assignment}</td>
          <td style="border: 1px solid #cbd5e1; padding:8px; text-align:center; font-family:monospace;">${s.participation}</td>
          <td style="border: 1px solid #cbd5e1; padding:8px; font-weight:bold; text-align:center; color:#2563eb; font-family:monospace;">${s.avg.toFixed(2)}</td>
          <td style="border: 1px solid #cbd5e1; padding:8px; font-weight:bold; text-align:center; color:#047857;">مستوفى</td>
        </tr>
      `).join('');

      documentContentsHTML = `
        <div style="font-size:15px; font-weight:800; color:#1e3a8a; margin-bottom:15px; text-align:center;">محضر النقاط والتقييمات للدفعة</div>
        <div class="meta-details" style="display:grid; grid-template-columns: 1fr 1fr; gap:10px; margin-bottom:20px; font-size:13px; font-weight:700;">
          <div>اسم الطالب ولقبه: <strong>${mockStudentObj.lastName} ${mockStudentObj.firstName}</strong></div>
          <div>رقم التسجيل: <strong style="font-family:monospace;">${mockStudentObj.id}</strong></div>
          <div>الفوج: <strong>${mockStudentObj.group}</strong></div>
          <div>السداسي: <strong>السداسي الثاني (S2)</strong></div>
        </div>
        <table class="saas-table" style="width:100%; border-collapse:collapse; font-size:12px;">
          <thead>
            <tr>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:center;">الرمز</th>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:right;">المقياس</th>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:center;">الامتحان</th>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:center;">الاعمال الموجهة</th>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:center;">الأعمال التطبيقية</th>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:center;">الواجبات</th>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:center;">المشاركة</th>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:center; color:#2563eb;">المعدل</th>
              <th style="border:1px solid #cbd5e1; padding:8px; background:#f8fafc; text-align:center;">النتيجة</th>
            </tr>
          </thead>
          <tbody>
            ${rows}
          </tbody>
        </table>
        <div style="font-size:14px; font-weight:800; text-align:left; margin-top:10px;">المعدل العام للفصل: <span style="color:#2563eb; font-family:monospace;">13.36 / 20</span></div>
      `;
    }

    const documentBody = `
      <!DOCTYPE html>
      <html lang="ar" dir="rtl">
      <head>
        <title>وثيقة أكاديمية رسمية</title>
        <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;800&display=swap" rel="stylesheet" />
        <style>
          body { font-family: 'Cairo', -apple-system, sans-serif; padding: 40px; color:#0f172a; direction: rtl; text-align: right; }
          .header-panel { display: flex; justify-content: space-between; align-items: center; border-bottom: 2.5px solid #2563eb; padding-bottom: 16px; margin-bottom: 30px; }
          .header-text { text-align: right; line-height: 1.6; }
          .university-logo { height: 80px; width: auto; }
          .footer-sign { display: flex; justify-content: flex-end; margin-top: 60px; font-size: 13px; font-weight: 700; color:#334155; }
        </style>
      </head>
      <body>
        <div class="header-panel">
          <div class="header-text">
            <div style="font-size: 13px; font-weight: 800; color:#0f172a;">الجمهورية الجزائرية الديمقراطية الشعبية</div>
            <div style="font-size: 12px; font-weight: 700; color:#475569; margin-top:4px;">وزارة التعليم العالي والبحث العلمي</div>
            <div style="font-size: 12px; font-weight: 700; color:#475569; margin-top:2px;">جامعة باتنة 1 – الحاج لخضر</div>
            <div style="font-size: 11px; color:#64748b; margin-top:2px;">كلية العلوم الإنسانية والاجتماعية</div>
            <div style="font-size: 11px; color:#64748b; margin-top:2px;">قسم: علم النفس</div>
          </div>
          <img src="https://www.univ-batna.dz/wp-content/uploads/2026/02/ChatGPT-Image-26-avr.-2026-16_54_46.png" class="university-logo" alt="Batna 1 University Logo" />
        </div>

        <div style="min-height:240px; padding:10px 0;">
          ${documentContentsHTML}
        </div>

        <div class="footer-sign">
          <div style="text-align: center; border-top: 1.5px dashed #cbd5e1; padding-top: 8px; min-width: 220px;">
            توقيع وختم رئيس القسم
          </div>
        </div>

        <script>
          window.addEventListener('load', function() {
            setTimeout(function() {
              window.print();
            }, 400);
          });
        <\/script>
      </body>
      </html>
    `;

    printWindow.document.write(documentBody);
    printWindow.document.close();
  }

  window.downloadSaaSReportArchive = function(refId) {
    // References maps
    const scopes = {
      DOC_48210: { template: 'Certificate of Enrollment', format: 'pdf' },
      DOC_10925: { template: 'Attendance Registry', format: 'csv' },
      DOC_09184: { template: 'Evaluations Summary', format: 'pdf' }
    };

    const log = scopes[refId];
    if (!log) return;

    selectedTemplate = log.template;
    selectedFormat = log.format;

    if (log.format === 'pdf') {
      compilePDFPrintLayout();
    } else {
      compileCSVOrExcelFile(log.format);
    }
  };

  // Spans floating green success toast alerts (Top-Left) [1]
  function triggerSuccessToast(title, description) {
    const container = document.getElementById('profileToastContainer');
    if (!container) return;

    const currentLang = localStorage.getItem('acadtrack-lang') || 'en';
    const toastId = 'toast_' + Date.now();

    const toastHTML = `
      <div class="saas-toast-alert" id="${toastId}">
        <div class="saas-toast-icon">
          <i class="fa-solid fa-circle-check"></i>
        </div>
        <div class="saas-toast-body">
          <div class="saas-toast-title">${title}</div>
          <div class="saas-toast-desc">${description}</div>
        </div>
      </div>
    `;

    container.innerHTML += toastHTML;

    // Slide-in transition [1]
    setTimeout(() => {
      const el = document.getElementById(toastId);
      if (el) el.classList.add('active');
    }, 50);

    // Auto Dismiss after 4 seconds
    setTimeout(() => {
      const el = document.getElementById(toastId);
      if (el) {
        el.classList.remove('active');
        setTimeout(() => el.remove(), 350);
      }
    }, 4000);
  }

  function translateSaaSElements(container) {
    if (!container) return;
    const currentLang = localStorage.getItem('acadtrack-lang') || 'en';
    container.querySelectorAll('[data-lang]').forEach(el => {
      el.style.display = el.getAttribute('data-lang') === currentLang ? '' : 'none';
    });
  }

  function escapeHtml(text) {
    if (text == null) return '';
    const div = document.createElement('div');
    div.textContent = String(text);
    return div.innerHTML;
  }
//]]>
</script>

</body>
</html>